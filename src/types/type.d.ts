type NavListItem = {
  id: number;
  title: string;
  link: string;
};

type mainNavList = {
  mainList: NavItem[];
  learnNow: NavItem[];
  fields: NavItem[];
};

type social = {
  id: number;
  title: string;
  link: string;
};
